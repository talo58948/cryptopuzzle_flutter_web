import 'package:flutter/material.dart';

class PuzzleWidget extends StatelessWidget {
  final bool isHovered;
  // final
  PuzzleWidget({this.isHovered = false});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
